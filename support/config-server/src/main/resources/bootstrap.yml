spring:
  application:
    name: config-server
server:
  port: 8888
---
spring:
   profiles: default
   cloud:
     config:
       server:
         git:
           uri: https://github.com/gamrani/config-server.git
           clone-on-start: true